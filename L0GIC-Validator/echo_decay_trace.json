import matplotlib.pyplot as plt

def plot_echo_trace(fragment_id, path="echo_decay_trace.json"):
    with open(path, "r") as f:
        data = json.load(f)

    trace = data.get(fragment_id, [])
    timestamps = [entry["timestamp"] for entry in trace]
    tones = [entry["tone"] for entry in trace]

    def apply_decay_resistance(fragment, decay_rate, model):
    if fragment.get("ritual_triggered", False):
        resistance = model.get("ritual_decay_resistance", 0.0)
        return round(decay_rate * (1 - resistance), 4)
    return decay_rate

    plt.plot(timestamps, tones, marker='o')
    plt.title(f"Echo Tone Drift: {fragment_id}")
    plt.xlabel("Time")
    plt.ylabel("Tone")
    plt.xticks(rotation=45)
    plt.tight_layout()
    plt.show()

{
  "fragment_id": "11-04-02",
  "echo_decay_trace": [
    {"timestamp": "2025-11-02T08:03:00Z", "tone": "awe"},
    {"timestamp": "2025-11-02T08:06:00Z", "tone": "soft-awe"},
    {"timestamp": "2025-11-02T08:09:00Z", "tone": "whisper"}
  ]
}
