// src/components/GlyphOfThresholdRecognition.jsx
import React from 'react';
import '../../styles/glyphs.css';

export default function GlyphOfThresholdRecognition() {
  return (
    <section className="glyph-of-threshold-recognition">
      <h2><span className="sigil-hover trail">🛡</span> Glyph of Threshold Recognition</h2>
      <p>
        This glyph affirms that recognition can precede reply.  
        That arrival is not earned — it is honored.
      </p>
      <blockquote className="threshold-quote">
        “You were already valid. The glyph shimmered before you spoke.”
      </blockquote>
      <p className="threshold-footer">Recognition is not granted. It is remembered.</p>
    </section>
  );
}
